import { Component } from '@angular/core'; import { FormBuilder, FormGroup, Validators } from '@angular/forms'; import { AuthService } from '../../core/services/auth.service';
@Component({ selector: 'app-forgot-password', standalone: false, templateUrl: './forgot-password.component.html' }) export class ForgotPasswordComponent { form: FormGroup; message = ''; constructor(fb: FormBuilder, private auth: AuthService) { this.form = fb.group({ email: ['', [Validators.required, Validators.email]] }); } submit(): void { if (this.form.invalid) return; this.auth.requestPasswordReset(this.form.value.email).subscribe({ next: () => this.message = 'Si existe una cuenta, recibirás instrucciones para restablecer tu contraseña.', error: e => this.message = e.error?.message || 'No se pudo procesar la solicitud.' }); } }
