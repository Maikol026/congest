import { Component, OnInit } from '@angular/core'; import { ActivatedRoute } from '@angular/router'; import { EstadoDeCuenta } from '../../core/models/estado-cuenta.model'; import { EstadosCuentaService } from '../../core/services/estados-cuenta.service';
@Component({ selector: 'app-estado-cuenta-detail', standalone: false, templateUrl: './estado-cuenta-detail.component.html' }) export class EstadoCuentaDetailComponent implements OnInit { estado?: EstadoDeCuenta; constructor(private route: ActivatedRoute, private service: EstadosCuentaService) {} ngOnInit(): void { this.service.getById(Number(this.route.snapshot.paramMap.get('id'))).subscribe({ next: d => this.estado = d }); } }
